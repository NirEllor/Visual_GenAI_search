# PyTorch (training, distillation, evaluation)
torch
torchvision

# Numerics & utilities
numpy
tqdm
matplotlib
Pillow
scikit-learn

# Metrics (optional — used with try/except fallback)
clean-fid
torch_fidelity

# Optional visualisation
# umap-learn  # pip install umap-learn  (enables --method umap in visualize_latents.py)

# JAX (CPU only — used for AE checkpoint loading and encoding/decoding)
jax
flax
optax
orbax-checkpoint
msgpack

# PyTorch (diffusion training, distillation, evaluation)
torch
torchvision

# Utilities
numpy
tqdm
matplotlib
scipy
Pillow
scikit-learn
# umap-learn  # optional: pip install umap-learn  (enables --method umap)

# Metrics
clean-fid
torch_fidelity
lpips
